
name: Test
on: 
 push:
    branches:
    - main
 pull_request:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checkout
      - name: Checkout repository
        uses: actions/checkout@v1
      
      - name: Upload Binary file to Google Drive folder MATSYA_REL_1_WITH_OEM
        uses: Jodebu/upload-to-drive@v1
        with:
          target: README.md
          credentials: ${{ secrets.G_DRIVE }}
          folder: 1IeUYoCrl1KF2DVRNwcd16swYYhd7qjho
